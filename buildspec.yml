version: 0.2

phases:
  install:
    runtime-versions:
        nodejs: 10
    commands:
    - echo Installing node packs...
    - yarn install
post_build:
  commands:
  - yarn build
  - echo Build completed on `date`
artifacts:
  files:
  - '**/*'
